version: "3"
services:
  app:
    restart: "always"
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/usr/app/pokedex
    ports:
      - "3000:3000"
    environment:
       - MONGO_DB_USER=beatriz
       - MONGO_DB_PASS=123